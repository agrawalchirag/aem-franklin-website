version: 1

indices:
  english:
    include:
      - /**
    exclude:
      - /nav
      - /header
      - /footer
      - /fragments/**
      - /docs/**
    target: /query-index.json
    properties:
